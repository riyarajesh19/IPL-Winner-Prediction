# IPL Winner Prediction (Machine Learning Project)
Built and compared ML models (Logistic Regression, Random Forest, SVM, and XGBoost) to predict IPL match outcomes.
Used data preprocessing, feature encoding, and GridSearchCV tuning — XGBoost delivered the best accuracy and insights for strategic decision-making.
